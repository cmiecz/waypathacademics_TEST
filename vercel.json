{
  "version": 2,
  "buildCommand": "npx expo export --platform web --output-dir dist && cp -r public/* dist/",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
